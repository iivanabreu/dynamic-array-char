# Dynamic array of characters

A simple way to make an array of characters in C using a dinamic size.

This simple code I made reallocs memory everytime the program reads an character and put its value in a string, this process continues until it reads a '\n'.
